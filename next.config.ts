/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true, // Next.js App Router yoqilganligiga ishonch hosil qiling
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
